---
layout: ../../layouts/NoteLayout.astro
title: "修改了影片下載功能，允許僅下載音訊，用於歌詞或 YouTube Music 內容。"
date: 2026-03-02T11:14
tags:
  - Python
  - API
  - Performance
source: "Jeffrey0117/ReelScript"
commit: "b177dca"
autoGenerated: true
---

- 引入 `content_type` 參數，允許指定下載影片或音訊。
- 針對 `lyrics` 類型的內容，使用 `youtube-dl` 的 `FFmpegExtractAudio` 後處理器提取音訊。
- 針對 `lyrics` 類型，調整 `youtube-dl` 的選項，僅選擇最佳音訊格式。
- 修改下載完成後的處理邏輯，對於 `lyrics` 類型跳過 `_ensure_h264` 步驟。
