---
layout: ../../layouts/NoteLayout.astro
title: "修正了在沒有選擇專案的情況下，遠端模式下的語音處理程序無法運作的問題。"
date: 2026-03-02T06:30
tags:
  - TypeScript
  - API
source: "Jeffrey0117/ClaudeBot"
commit: "ddb7af5"
autoGenerated: true
---

- 修正在沒有選定專案時，遠端配對模式下語音處理的判斷邏輯，新增了對 `getPairing(chatId, threadId)?.connected` 的判斷，允許遠端配對使用語音功能。
- 修改 `hasProjectOrAsrMode` 函數，增加對 `pairing?.connected === true` 的判斷，允許在遠端配對已連接的情況下通過檢查。
- 修改後的程式碼邏輯更嚴謹，考慮了更多使用情境，提升了程式碼的健壯性。
