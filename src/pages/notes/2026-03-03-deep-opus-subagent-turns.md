---
layout: ../../layouts/NoteLayout.astro
title: "新增 `/deep` 指令，用於深度分析模式，使用 opus 模型和 subagent 分析，並允許設定最大turns。"
date: 2026-03-03T22:14
tags:
  - TypeScript
  - API
  - AI
  - Architecture
source: "Jeffrey0117/ClaudeBot"
commit: "52fad4f"
autoGenerated: true
---

- 引入 `/deep` 指令，方便使用者進行深度分析，該指令會使用 `opus` 模型，並指示 AI 使用 subagent 從多個角度分析問題。
- 增加 `maxTurns` 參數，允許使用者或程式碼設定 AI 運行的最大 turns，並在 `deepCommand` 中將預設值加倍，以應付深度分析的需求。
- 修改 `reloadCtxSpec` 為 `reloadAllSpecs`，可同時重新載入 `ctx-spec.md` 和 `subagent-spec.md`，方便更新設定。
