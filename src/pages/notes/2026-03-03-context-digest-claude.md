---
layout: ../../layouts/NoteLayout.astro
title: "引入 Context Digest 機制，讓 Claude 能夠維護結構化的工作記憶，並在後續對話中利用這些記憶。"
date: 2026-03-03T21:26
tags:
  - TypeScript
  - AI
  - Architecture
source: "Jeffrey0117/ClaudeBot"
commit: "673b030"
autoGenerated: true
---

- 在 System Prompt 中定義了 Context Digest 的格式和規則，要求 Claude 在每次回應的結尾添加 `[CTX]...[/CTX]` 區塊，包含狀態、摘要和待決事項。
- 新增 `context-digest-store.ts` 模組，用於解析、儲存和檢索 Context Digest。這個模組可以從 Claude 的回應中提取 `[CTX]` 區塊，並將其結構化地儲存起來，以便後續使用。
- 修改 `queue-processor.ts`，在處理 Claude 的回應時，提取 Context Digest，並將其儲存到 `context-digest-store` 中。同時，從顯示文字中移除 `[CTX]` 區塊。
- 修改 `claude-runner.ts`，在使用者發送簡短或肯定回覆時，將儲存的 Context Digest 注入到 prompt 中，以便 Claude 能夠理解使用者的意圖。如果沒有 Context Digest，則會使用原始的回應尾部作為 fallback。
- 引入 `buildContextInjection` 函數，根據 Context Digest 的狀態和回覆類型，生成特定的 action hint。這個 hint 可以幫助 Claude 更好地理解使用者的意圖，並採取相應的行動。
