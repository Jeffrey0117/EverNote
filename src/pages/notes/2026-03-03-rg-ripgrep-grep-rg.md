---
layout: ../../layouts/NoteLayout.astro
title: "使用 `rg` (ripgrep) 作為 `grep` 的替代方案，如果 `rg` 可用，則使用 `rg`，否則使用 `grep`。`rg` 優點：自動遵守 `.gitignore`，速度更快，正則表達式更好。"
date: 2026-03-03T22:29
tags:
  - TypeScript
  - Performance
  - Git
source: "Jeffrey0117/ClaudeBot"
commit: "23cd411"
autoGenerated: true
---

- 引入 `ripgrep` 作為 `grep` 的替代方案，並提供 fallback 機制。
- 使用 `exec` 執行 shell 命令，並使用 `Promise` 包裝以進行異步處理。
- 新增 `checkRgAvailable` 函數，用於檢查 `rg` 是否可用，並將結果緩存在 `rgAvailable` 變量中。
- 新增 `buildRgCommand` 和 `buildGrepCommand` 函數，用於構建 `rg` 和 `grep` 命令。
