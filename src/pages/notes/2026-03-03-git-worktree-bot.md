---
layout: ../../layouts/NoteLayout.astro
title: "新增 Git worktree 支援，允許多個 bot 實例同時在同一專案的不同分支上工作，並提供合併至主分支的指令。"
date: 2026-03-03T15:33
tags:
  - TypeScript
  - Git
  - DevOps
  - Architecture
source: "Jeffrey0117/ClaudeBot"
commit: "39bc693"
autoGenerated: true
---

- 引入 git worktree 的概念，實現多個 bot 實例在隔離的環境中並行開發。
- 實作 `ensureWorktree()` 自動建立或重用 worktree，`mergeToMain()` 將 worktree 分支合併回主分支。
- 修改 `deployCommand`，在 worktree 環境下部署時，先將分支合併回 master，再推送 master 分支。
- 修改 `findProject` 和 `detectProjectMention`，使其能正確解析 worktree 路徑。
- 新增 `isWorktree()` 判斷目錄是否為 worktree，`mainRepoPath()` 取得主倉庫路徑。
