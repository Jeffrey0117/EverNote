---
layout: ../../layouts/NoteLayout.astro
title: "新增 `/ctx` 指令，用於查看和管理上下文摘要，並擴充了 CTX 規格以包含 `next` 和 `files` 欄位，同時支援熱重載 `ctx-spec.md`。"
date: 2026-03-03T21:45
tags:
  - TypeScript
  - API
  - Architecture
  - AI
source: "Jeffrey0117/ClaudeBot"
commit: "7c62dac"
autoGenerated: true
---

- 引入了 `/ctx` 指令，方便使用者查看和管理上下文摘要，提升了互動體驗。
- 擴充了 `ContextDigest` 介面，新增 `next` 和 `files` 欄位，使上下文資訊更完整。
- 實作了熱重載 `ctx-spec.md` 的功能，方便開發者快速更新和測試 CTX 規格。
- 使用正則表達式 (`CTX_REGEX`) 從 Claude 的回覆中提取上下文摘要，並提供相應的指示。
- 在構建上下文注入時，根據不同的 `digest.status` 提供不同的指示，提升了 AI 的理解能力。
